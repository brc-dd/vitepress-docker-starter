# Markdown Demo

A small sample of what VitePress renders out of the box.

## Code

```ts
const site = {
  dev: 'http://localhost:5173',
  prod: 'http://localhost:8080'
}
```

## Containers

::: tip
This page exists mainly to demo a flat clean URL: it is `markdown.md`, served at `/markdown`.
:::

See the [VitePress markdown reference](https://vitepress.dev/guide/markdown) for everything else.
