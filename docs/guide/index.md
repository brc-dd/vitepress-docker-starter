# Overview

This starter runs [VitePress](https://vitepress.dev) in Docker two ways:

- **Dev** — `docker compose up dev`, then edit files under `docs/` with hot reload.
- **Prod** — `docker compose up prod --build`, a static build served by nginx.

See [Getting Started](/guide/getting-started) for the commands, and the repository README for how the pieces fit together.
